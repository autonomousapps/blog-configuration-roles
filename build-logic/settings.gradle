pluginManagement {
  plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.9.10'
  }
}

dependencyResolutionManagement {
  repositories {
    mavenCentral()
  }
}

rootProject.name = 'configuration-roles-plugin'

include ':configuration-roles'
include ':module-rules'
