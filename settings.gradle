pluginManagement {
  includeBuild 'build-logic'
}

dependencyResolutionManagement {
  repositories {
    mavenCentral()
  }
}

rootProject.name = 'configuration-roles-sample'

include ':aggregator'
include ':magic'
include ':heart-of-gold'
